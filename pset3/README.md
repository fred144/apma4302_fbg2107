# PSET3

the write up can be found in `writeup/main.pdf`.

Each questions has a bash script and STDOUT, with relevant extra files (like logs) in the `qX_results` folders. 

The plotting was done in python for relevant questions, and the code is in `plot_qX.py` files. The final plots are in the `qX_results` folders.